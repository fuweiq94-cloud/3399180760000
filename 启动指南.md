# 🚀 D3QN Snake AI 启动指南

## ✅ 环境已创建成功！

虚拟环境已使用 `uv` 创建在 `.venv` 目录中，基于 Python 3.14。

---

## 📋 启动方式

### 方式 1: 使用 PowerShell 脚本（推荐）⭐

```powershell
.\start.ps1 -Train
```

**功能特点：**
- ✅ 自动检测 `.venv` 是否存在
- ❌ 不存在则自动创建
- ✅ 安装核心依赖（torch, numpy, gymnasium, matplotlib）
- ⚠️ 注意：pygame 需要手动安装（见下方说明）

**其他参数：**
```powershell
.\start.ps1 -Install    # 仅安装依赖
.\start.ps1 -Demo       # 运行演示模式
.\start.ps1             # 无参数直接运行（默认训练）
```

---

### 方式 2: 使用批处理文件

```batch
.\start_training.bat
```

**功能特点：**
- ✅ Windows CMD 兼容性更好
- ✅ 自动检查并激活虚拟环境
- ✅ 简单可靠

---

## ⚠️ pygame 安装问题

由于您的系统使用 **Python 3.14**，pygame 遇到了兼容性问题。有以下几种解决方案：

### 方案 A: 使用离线安装（最简单）

1. **下载预编译的 wheel 包**：
   ```bash
   # 访问网站下载适合您系统的版本：
   https://www.lfd.uci.edu/~gohlke/pythonlibs/#pygame
   
   # 或直接访问 PyPI 查看可用版本
   https://pypi.org/project/pygame/#files
   ```

2. **下载对应的 .whl 文件后安装**：
   ```bash
   cd d:\zm\D3QN\.venv\Lib\site-packages
   pip install pygame-*.whl
   ```

### 方案 B: 降低 Python 版本（不推荐但最稳定）

如果您希望立即开始训练，建议使用更稳定的 Python 3.11 或 3.10：

```bash
# 安装 Python 3.11
uv venv --python 3.11

# 重新安装所有依赖
uv pip install torch numpy gymnasium pygame matplotlib
```

### 方案 C: 暂时移除 pygame（用于纯训练）

修改 `snake_env.py`，注释掉 render() 方法中的 pygame 调用，这样训练可以正常运行，只是没有可视化界面。

---

## 🔧 验证安装是否成功

打开 PowerShell，进入项目目录：

```powershell
cd d:\zm\D3QN

# 检查虚拟环境
if (Test-Path ".venv") {
    Write-Host "✓ Virtual environment exists"
    
    # 测试导入 PyTorch
    & .venv\Scripts\python.exe -c "import torch; print('PyTorch:', torch.__version__)"
    
    # 测试导入其他依赖
    & .venv\Scripts\python.exe -c "import numpy; import gymnasium; print('Core dependencies OK')"
} else {
    Write-Host "✗ Virtual environment not found!" -ForegroundColor Red
}
```

---

## 🎯 完整启动流程（推荐）

### Step 1: 确保虚拟环境存在

```powershell
cd d:\zm\D3QN

if (-not (Test-Path ".venv")) {
    uv venv --python 3.14
}
```

✅ 当前状态：**已完成** ✓

### Step 2: 安装核心依赖（使用 uv）

```powershell
# 已经在虚拟环境中安装了 torch, numpy, gymnasium, matplotlib
# 通过之前的命令完成：uv pip install torch numpy gymnasium matplotlib
```

✅ 当前状态：**已完成** ✓

### Step 3: 解决 pygame 问题（选择一种方案）

根据您的情况选择一个方案：

**如果希望继续用 Python 3.14：**
- 尝试手动下载安装 pygame wheel
- 或者暂时移除 render 功能

**如果想快速开始训练：**
```powershell
# 重新创建 Python 3.11 环境（需要您自己下载并安装 Python 3.11）
uv venv --python 3.11
uv pip install torch numpy gymnasium pygame matplotlib
```

### Step 4: 启动训练

```powershell
# 使用 PowerShell 脚本
.\start.ps1 -Train

# 或使用批处理文件
.\start_training.bat
```

---

## 📊 预期输出

如果一切正常，您将看到：

```
[OK] Virtual environment detected: .venv
Starting D3QN Snake AI Training...

============================================================
🐍 D3QN Snake Training Started!
============================================================
Target episodes: 5000
Device: cpu        # 或 cuda 如果有 GPU
Epsilon decay: 0.995
Save interval: every 50 episodes
============================================================

Episode:     1 | Epsilon: 1.0000 | Avg Reward:  -8.10 | Score:   0 | Steps:  500

Episode:    10 | Epsilon: 0.6065 | Avg Reward:  -5.23 | Score:   2 | Steps: 1234
...
```

---

## 🔍 故障排查

### 问题 1: "Virtual environment not found"

**原因**: `.venv` 文件夹不存在

**解决方法**:
```powershell
uv venv --python 3.14
```

### 问题 2: "ModuleNotFoundError: No module named 'torch'"

**原因**: 依赖未安装或未激活虚拟环境

**解决方法**:
```powershell
# 确保在项目目录下
cd d:\zm\D3QN

# 重新安装
uv pip install torch numpy gymnasium matplotlib
```

### 问题 3: pygame 安装失败

**原因**: Python 3.14 与 pygame 兼容性问题

**解决方法**: 见上方"pygame 安装问题"章节

### 问题 4: "Permission denied" 错误

**原因**: PowerShell 执行策略限制

**解决方法**:
```powershell
# 临时允许运行脚本
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\start.ps1 -Train
```

---

## 💡 提示和技巧

1. **GPU 加速**：如果 CUDA 可用，PyTorch 会自动使用 GPU，速度会快很多
2. **断点续训**：模型会每 50 个回合保存一次，可以随时加载继续训练
3. **监控训练进度**：生成的 `training_curves.png` 可以看到详细的训练曲线
4. **调整参数**：编辑 `train.py` 中的参数来控制训练行为

---

## 📁 重要文件说明

- `.venv/` - 虚拟环境目录（不要删除）
- `models/` - 保存的训练好的模型
- `training_curves.png` - 自动生成的训练图表
- `train.py` - 主训练脚本
- `demo.py` - 测试脚本
- `start.ps1` - PowerShell 启动器 ⭐推荐
- `start_training.bat` - Batch 启动器

---

## ✨ 下一步

虚拟环境已经创建完成！请按照上述步骤解决 pygame 问题后，就可以开始训练了！

如有任何问题，随时询问！🐍🤖
